[package]
name = "stembed-wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
stembed = { path = "../stembed" }
console_error_panic_hook = "0.1.7"
wasm-bindgen = "0.2.100"
